


  SET_LINES: (state, { lines }) => {
    state.lines = lines
  },
  SET_VIEW: (state, { view }) => {
    state.view = view
  },
  SET_OPTIONS: (state, { options }) => {
    state.options = options
  }
}
