export const SET_OPTIONS = function (state, { data }) {
  state.options = data
}

export const SET_PRODUCTS = function (state, { data }) {
  state.products = data
}

export const SET_GROUPS = function (state, { data }) {
  state.groups = data
}

export const SET_FAMILIES = function (state, { data }) {
  state.families = data
}

export const SET_FAMILY = function (state, { data }) {
  state.family = data
}

export const SET_COLLECTIONS = function (state, { data }) {
  state.collections = data
}
